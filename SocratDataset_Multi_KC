[
  {
    "id": "task_001_multi_debug",
    "problem_description": "编写一个函数 `sum_even_squares(nums)`，计算列表内所有偶数的平方和。当前代码无法运行且逻辑存在错误。",
    "buggy_code": "def sum_even_squares(nums):\n    res = 0\n    for n in nums:\n        if n % 2 = 0:\n            res += n * 2\n    return res",
    "kcs": [
      {
        "kc_id": "kc_syntax_eq",
        "description": "理解 Python 中条件判断应当使用双等号 '==' 而不是赋值运算符 '='。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_logic_square",
        "description": "理解平方的数学运算在代码中应表示为 n**2 或 n*n，而不是 n*2。",
        "prerequisites": ["kc_syntax_eq"]
      }
    ]
  },
  {
    "id": "task_002_multi_debug",
    "problem_description": "编写一个函数 `max_student_score(students)`，接收一个包含多个字典的列表（如 `[{'name': 'Alice', 'score': 90}]`），返回其中的最高分。如果列表为空，应安全返回 None。",
    "buggy_code": "def max_student_score(students):\n    max_val = max(students, key=lambda x: x.score)\n    return max_val",
    "kcs": [
      {
        "kc_id": "kc_dict_key_access",
        "description": "理解字典中的值必须通过中括号 `x['score']` 访问，而不能像对象属性那样使用点号 `.score`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_max_empty_sequence",
        "description": "理解 `max()` 函数在处理空序列时会抛出 ValueError，必须通过设置 `default=None` 参数或提前判断空列表来处理。",
        "prerequisites": ["kc_dict_key_access"]
      }
    ]
  },
  {
    "id": "task_003_multi_debug",
    "problem_description": "编写一个函数 `remove_dups_preserve_order(lst)`，去除列表中的重复元素，同时保持原有的顺序。",
    "buggy_code": "def remove_dups_preserve_order(lst):\n    seen = []\n    for item in lst:\n        if item not in seen\n            seen.add(item)\n    return seen",
    "kcs": [
      {
        "kc_id": "kc_syntax_colon",
        "description": "识别并修复 `if` 语句末尾缺失冒号 `:` 的基础语法错误。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_list_append_method",
        "description": "理解 `seen` 是一个列表(List)，向列表追加元素必须使用 `.append()` 方法，而不是集合(Set)的 `.add()` 方法。",
        "prerequisites": ["kc_syntax_colon"]
      }
    ]
  },
  {
    "id": "task_004_multi_debug",
    "problem_description": "编写一个函数 `word_count(sentence)`，统计英文句子中每个单词的出现频率，返回一个字典。注意句子中可能包含多个连续的空格。",
    "buggy_code": "def word_count(sentence):\n    counts = {}\n    words = sentence.split(\" \")\n    for w in words:\n        counts[w] = counts[w] + 1\n    return counts",
    "kcs": [
      {
        "kc_id": "kc_dict_key_error",
        "description": "理解在字典中直接累加一个不存在的键会触发 KeyError，需要先进行键存在性检查或使用 `get()` / `defaultdict`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_split_whitespace",
        "description": "理解使用 `.split(\" \")` 会保留多余的空字符串，而直接使用 `.split()` 才能正确以任意空白字符分割并自动过滤空字符串。",
        "prerequisites": ["kc_dict_key_error"]
      }
    ]
  },
  {
    "id": "task_005_multi_debug",
    "problem_description": "编写一个函数 `add_student_to_class(classes_dict, class_name, student)`。将学生添加到对应班级的列表中，如果班级不存在则自动创建该班级。最后返回更新后的字典。",
    "buggy_code": "def add_student_to_class(classes_dict, class_name, student):\n    classes_dict.get(class_name, []).append(student)\n    return classes_dict",
    "kcs": [
      {
        "kc_id": "kc_dict_get_mutation_lost",
        "description": "理解 `dict.get(key, [])` 当键不存在时返回的是一个临时的新列表，对其 append 不会修改原字典。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_dict_setdefault",
        "description": "掌握如何使用 `classes_dict.setdefault(class_name, []).append(student)` 或通过 `if` 语句来持久化更新字典的值。",
        "prerequisites": ["kc_dict_get_mutation_lost"]
      }
    ]
  },
  {
    "id": "task_006_multi_debug",
    "problem_description": "编写一个函数 `reverse_string(s)`，接收一个字符串并返回它的反转字符串。",
    "buggy_code": "def reverse_string(s):\n    s.reverse()\n    return s",
    "kcs": [
      {
        "kc_id": "kc_string_immutability",
        "description": "理解字符串在 Python 中是不可变对象 (Immutable)，并没有像列表那样的 `.reverse()` 就地修改方法。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_slice_reversal",
        "description": "掌握反转字符串的正确语法，如使用切片 `s[::-1]` 或 `''.join(reversed(s))`。",
        "prerequisites": ["kc_string_immutability"]
      }
    ]
  },
  {
    "id": "task_007_multi_debug",
    "problem_description": "编写一个函数 `avg_positives(nums)`，计算并返回列表中所有大于 0 的数字的平均值。如果列表中没有正数，请返回 0。",
    "buggy_code": "def avg_positives(nums):\n    positives = [n for n in nums if n > 0]\n    return sum(positives) / len(nums)",
    "kcs": [
      {
        "kc_id": "kc_logic_average_denominator",
        "description": "理解求平均值的除数应当是符合条件的元素个数 `len(positives)`，而不是原列表的总长度 `len(nums)`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_zero_division",
        "description": "预见当没有符合条件的元素时，列表长度为 0 会导致 ZeroDivisionError 崩溃，需增加提前返回 0 的守卫逻辑。",
        "prerequisites": ["kc_logic_average_denominator"]
      }
    ]
  },
  {
    "id": "task_008_multi_debug",
    "problem_description": "编写一个函数 `all_even(nums)`，判断列表中是否所有数字都是偶数。如果是返回 True，只要有一个奇数就返回 False。空列表默认返回 True。",
    "buggy_code": "def all_even(nums):\n    for n in nums:\n        if n % 2 == 0:\n            return True\n        else:\n            return False",
    "kcs": [
      {
        "kc_id": "kc_loop_premature_return",
        "description": "理解在 `for` 循环中不能在遇到第一个符合条件的元素时就直接 `return True`，这样会导致后续元素未被检查。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_empty_list_semantics",
        "description": "理解当列表为空时，循环体不会执行，函数默认会返回 None，必须在循环体外加上 `return True` 兜底。",
        "prerequisites": ["kc_loop_premature_return"]
      }
    ]
  },
  {
    "id": "task_009_multi_debug",
    "problem_description": "编写一个函数 `merge_dicts_sum(d1, d2)`。合并两个字典，如果某个键在两个字典中都存在，则将其对应的值相加。",
    "buggy_code": "def merge_dicts_sum(d1, d2):\n    res = d1.copy()\n    for k, v in d2:\n        if k in res:\n            res[k] =+ v\n        else:\n            res[k] = v\n    return res",
    "kcs": [
      {
        "kc_id": "kc_dict_items_iteration",
        "description": "理解在 `for` 循环中直接解包字典 `d2` 会触发 ValueError，必须使用 `d2.items()` 才能同时获取键和值。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_syntax_augmented_assignment",
        "description": "识别 `res[k] =+ v` 是一个危险的隐蔽语法错误，它等同于赋值正数 `v`，正确的累加语法必须是 `+=`。",
        "prerequisites": ["kc_dict_items_iteration"]
      }
    ]
  },
  {
    "id": "task_010_multi_debug",
    "problem_description": "编写一个函数 `first_duplicate_insensitive(s)`，寻找字符串中第一个重复出现的字符（忽略大小写，即 'A' 和 'a' 算作重复）。如果找到返回该小写字符，没找到返回 None。",
    "buggy_code": "def first_duplicate_insensitive(s):\n    seen = set()\n    for char in s:\n        if char in seen:\n            return char\n        seen.append(char)\n    return None",
    "kcs": [
      {
        "kc_id": "kc_set_add_vs_append",
        "description": "理解向集合 (Set) 中添加元素必须使用 `.add()` 方法，而不是列表的 `.append()`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_case_sensitivity",
        "description": "理解忽略大小写的判断需要在比较前将字符统一转换为小写 `char.lower()`。",
        "prerequisites": ["kc_set_add_vs_append"]
      }
    ]
  },
  {
    "id": "task_011_multi_debug",
    "problem_description": "编写一个函数 `clean_and_join(words)`，接收一个字符串列表。将每个字符串中的空格删除，然后用逗号将它们连接成一个长字符串，并去掉末尾多余的逗号。",
    "buggy_code": "def clean_and_join(words):\n    res = \"\"\n    for w in words:\n        w.replace(\" \", \"\")\n        res += w + \",\"\n    res.rstrip(\",\")\n    return res",
    "kcs": [
      {
        "kc_id": "kc_string_immutable_replace",
        "description": "理解字符串是不可变的，`w.replace()` 不会原地修改 `w`，而是返回一个新字符串，必须将结果重新赋值（如 `w = w.replace(...)`）。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_string_immutable_rstrip",
        "description": "同理，理解 `res.rstrip(\",\")` 也不会改变原字符串，必须写成 `res = res.rstrip(\",\")` 才能使末尾的逗号真正被剥离。",
        "prerequisites": ["kc_string_immutable_replace"]
      }
    ]
  },
  {
    "id": "task_012_multi_debug",
    "problem_description": "编写一个函数 `group_by_first_letter(words)`。将单词列表按首字母分组，返回一个字典，键是首字母，值是包含对应单词的列表。注意列表中可能包含空字符串 \"\"。",
    "buggy_code": "def group_by_first_letter(words):\n    groups = {}\n    for w in words:\n        first = w[0]\n        groups[first].append(w)\n    return groups",
    "kcs": [
      {
        "kc_id": "kc_empty_string_index",
        "description": "理解当 `w` 是空字符串 `\"\"` 时，执行 `w[0]` 会触发 IndexError，需要先判断单词是否非空。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_dict_keyerror_append",
        "description": "理解直接访问字典中不存在的键会触发 KeyError，需要初始化列表，例如使用 `groups.setdefault(first, []).append(w)` 或 `collections.defaultdict`。",
        "prerequisites": ["kc_empty_string_index"]
      }
    ]
  },
  {
    "id": "task_013_multi_debug",
    "problem_description": "编写一个函数 `remove_evens(nums)`，在原列表中移除所有的偶数，并返回修改后的列表。",
    "buggy_code": "def remove_evens(nums):\n    for num in nums:\n        if num % 2 == 0:\n            nums.remove(num)\n    return nums",
    "kcs": [
      {
        "kc_id": "kc_modify_list_during_iteration",
        "description": "理解在正向遍历列表的同时直接使用 `.remove()` 删除元素，会导致迭代器的索引发生错位，从而跳过相邻的元素。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_iterate_over_copy",
        "description": "掌握安全删除元素的方案，例如遍历列表的拷贝 `for num in nums[:]`，或者更 Pythonic 的做法：使用列表推导式覆盖原变量。",
        "prerequisites": ["kc_modify_list_during_iteration"]
      }
    ]
  },
  {
    "id": "task_014_multi_debug",
    "problem_description": "编写一个函数 `add_item(item, my_list=[])`，将元素添加到列表中并返回。如果没有传入列表，则默认创建一个新列表。",
    "buggy_code": "def add_item(item, my_list=[]):\n    my_list.append(item)\n    return my_list",
    "kcs": [
      {
        "kc_id": "kc_mutable_default_args",
        "description": "理解 Python 中的默认参数在函数定义时只会被求值一次。如果默认参数是可变对象（如列表），多次调用函数会导致状态被意外累加（共享同一个列表）。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_none_check_for_defaults",
        "description": "掌握修复可变默认参数的标准模式：在参数中使用 `my_list=None`，并在函数体内部执行 `if my_list is None: my_list = []`。",
        "prerequisites": ["kc_mutable_default_args"]
      }
    ]
  },
  {
    "id": "task_015_multi_debug",
    "problem_description": "实现一个全局计数器。定义全局变量 `count = 0`，并编写函数 `increment_counter()`，每次调用让计数器加 1 并返回新值。",
    "buggy_code": "count = 0\ndef increment_counter():\n    count = count + 1\n    return count",
    "kcs": [
      {
        "kc_id": "kc_local_unbound_error",
        "description": "理解在函数内部尝试给同名变量赋值时，Python 会将其视为局部变量。由于在赋值前使用了 `count + 1`，会触发 UnboundLocalError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_global_keyword",
        "description": "掌握在函数内部修改全局变量的正确方式，必须在函数首行使用 `global count` 进行显式声明。",
        "prerequisites": ["kc_local_unbound_error"]
      }
    ]
  },
  {
    "id": "task_016_multi_debug",
    "problem_description": "编写一个函数 `sort_people(people)`，接收一个字典列表。按照字典中 'age' 键的值对列表进行降序排序。注意有些字典可能没有 'age' 键，没有的当做年龄为 0 处理。",
    "buggy_code": "def sort_people(people):\n    people.sort(key='age', reverse=True)\n    return people",
    "kcs": [
      {
        "kc_id": "kc_sort_key_callable",
        "description": "理解列表 `sort` 方法的 `key` 参数必须接收一个可调用对象（函数或 lambda），而不能直接传入字符串 'age'。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_missing_dict_key_in_sort",
        "description": "预见当字典缺失 'age' 键时，简单的 `lambda x: x['age']` 会触发 KeyError，必须使用 `lambda x: x.get('age', 0)`。",
        "prerequisites": ["kc_sort_key_callable"]
      }
    ]
  },
  {
    "id": "task_017_multi_debug",
    "problem_description": "编写一个 `BankAccount` 类，包含一个 `deposit(amount)` 方法用于存钱。每个实例对象应该维护自己的余额。",
    "buggy_code": "class BankAccount:\n    balance = 0\n    def deposit(self, amount):\n        BankAccount.balance += amount",
    "kcs": [
      {
        "kc_id": "kc_class_vs_instance_var",
        "description": "理解类变量 (`BankAccount.balance`) 会被所有实例共享，修改它会导致所有用户的余额一起变动，这是极其危险的逻辑错误。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_self_initialization",
        "description": "掌握如何使用 `__init__` 构造方法，并通过 `self.balance = 0` 来初始化属于实例私有的属性。",
        "prerequisites": ["kc_class_vs_instance_var"]
      }
    ]
  },
  {
    "id": "task_018_multi_debug",
    "problem_description": "编写一个函数 `parse_number(s)`，尝试将字符串转换为整数并返回。如果转换失败（比如输入了字母），则返回 0。",
    "buggy_code": "def parse_number(s):\n    try:\n        return int(s)\n    except:\n        return 0",
    "kcs": [
      {
        "kc_id": "kc_bare_except_danger",
        "description": "理解裸露的 `except:` 会捕获所有异常（包括系统退出 SystemExit 和键盘中断 KeyboardInterrupt），这在工业界是绝对禁止的反模式。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_specific_exception",
        "description": "掌握异常处理的最佳实践：只捕获预期的异常。在这个例子中，必须明确写出 `except ValueError:`。",
        "prerequisites": ["kc_bare_except_danger"]
      }
    ]
  },
  {
    "id": "task_019_multi_debug",
    "problem_description": "编写一个函数 `sum_of_squares(n)`，计算从 0 到 n-1 所有数字的平方和。注意，测试用例中的 n 会非常大（如 10 的 8 次方），需要考虑内存消耗。",
    "buggy_code": "def sum_of_squares(n):\n    squares = [x**2 for x in range(n)]\n    return sum(squares)",
    "kcs": [
      {
        "kc_id": "kc_list_comp_memory",
        "description": "理解列表推导式 `[...]` 会在内存中一次性实例化所有元素。当 n 极大时，会导致 MemoryError (内存溢出) 崩溃。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_generator_expression",
        "description": "掌握通过将中括号 `[]` 替换为小括号 `()`，将其转换为生成器表达式，从而实现惰性求值 (Lazy Evaluation)，大幅节省内存。",
        "prerequisites": ["kc_list_comp_memory"]
      }
    ]
  },
  {
    "id": "task_020_multi_debug",
    "problem_description": "编写一个函数 `greet(name)`。如果 `name` 有内容，则返回 \"Hello, [name]\"；如果 `name` 是空字符串，则默认返回 \"Hello, Guest\"。",
    "buggy_code": "def greet(name):\n    valid_name = name == True and name or \"Guest\"\n    return f\"Hello, {valid_name}\"",
    "kcs": [
      {
        "kc_id": "kc_boolean_identity_comparison",
        "description": "理解 `name == True` 是严格类型比较，如果 `name` 是字符串 \"Alice\"，\"Alice\" == True 的结果是 False，导致逻辑瘫痪。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_truthy_falsy_or",
        "description": "掌握 Python 中的 Truthy/Falsy 短路求值特性。非空字符串自带 Truthy 属性，最优雅的写法是直接使用 `valid_name = name or \"Guest\"`。",
        "prerequisites": ["kc_boolean_identity_comparison"]
      }
    ]
  },
  {
    "id": "task_021_multi_debug",
    "problem_description": "编写一个函数 `flatten_and_sum(nested_list)`，接收一个包含多个整数子列表的列表（如 `[[1, 2], [3, 4]]`）。将其展平并返回所有数字的总和。",
    "buggy_code": "def flatten_and_sum(nested_list):\n    flat = []\n    for sub in nested_list:\n        flat.append(sub)\n    return sum(flat)",
    "kcs": [
      {
        "kc_id": "kc_list_append_vs_extend",
        "description": "理解 `append()` 会将子列表作为一个完整的对象加入，导致 `flat` 依然是嵌套列表；而展平必须使用 `extend()` 或 `+=`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_sum_type_error",
        "description": "理解由于 `flat` 没有被正确展平，里面装的是列表对象。调用 `sum()` 时会尝试执行 `0 + [1, 2]`，从而抛出 TypeError。",
        "prerequisites": ["kc_list_append_vs_extend"]
      }
    ]
  },
  {
    "id": "task_022_multi_debug",
    "problem_description": "编写一个函数 `format_profile(name, age)`。接收姓名（字符串）和年龄（整数），返回格式化字符串 \"Name: [name], Age: [age]\"。",
    "buggy_code": "def format_profile(name, age):\n    return \"Name: \" + name + \", Age: \" + age",
    "kcs": [
      {
        "kc_id": "kc_string_concat_typeerror",
        "description": "理解在 Python 中，不能直接使用 `+` 运算符将字符串 (str) 和整数 (int) 拼接，否则会抛出 TypeError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_fstring_formatting",
        "description": "掌握修复类型拼接错误的现代方法：使用 f-string (如 `f\"Name: {name}, Age: {age}\"`) 或显式调用 `str(age)`。",
        "prerequisites": ["kc_string_concat_typeerror"]
      }
    ]
  },
  {
    "id": "task_023_multi_debug",
    "problem_description": "编写一个函数 `filter_high_values(scores_dict)`，接收一个姓名到分数的字典（如 `{'Alice': 95, 'Bob': 70}`），返回所有大于 80 的分数的列表。",
    "buggy_code": "def filter_high_values(scores_dict):\n    res = []\n    for v in scores_dict:\n        if v > 80:\n            res.append(v)\n    return res",
    "kcs": [
      {
        "kc_id": "kc_dict_iteration_keys",
        "description": "理解直接使用 `for v in dict` 遍历字典时，迭代的是字典的【键(Key)】（通常是字符串），而不是【值(Value)】。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_typeerror_str_int",
        "description": "预见因为迭代的是字符串键，执行 `v > 80` 时会触发类型错误。必须将迭代方式改为 `scores_dict.values()`。",
        "prerequisites": ["kc_dict_iteration_keys"]
      }
    ]
  },
  {
    "id": "task_024_multi_debug",
    "problem_description": "编写一个函数 `append_to_tuple(tup, val)`，接收一个元组和一个数字，返回一个将数字追加到末尾的新元组。",
    "buggy_code": "def append_to_tuple(tup, val):\n    return tup + (val)",
    "kcs": [
      {
        "kc_id": "kc_tuple_single_element_comma",
        "description": "理解 `(val)` 只是一个带括号的数字，并不是元组。定义单元素元组必须在末尾加逗号 `(val,)`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_tuple_concat_typeerror",
        "description": "理解元组的 `+` 拼接操作要求两边都是元组对象，否则抛出 TypeError。必须用逗号将对象包装为元组才能拼接。",
        "prerequisites": ["kc_tuple_single_element_comma"]
      }
    ]
  },
  {
    "id": "task_025_multi_debug",
    "problem_description": "编写一个函数 `is_vowel(char)`。如果是元音字母 (a, e, i, o, u)，则返回 True，否则返回 False。",
    "buggy_code": "def is_vowel(char):\n    if char == 'a' or 'e' or 'i' or 'o' or 'u':\n        return True\n    return False",
    "kcs": [
      {
        "kc_id": "kc_or_operator_truthy",
        "description": "理解 `if` 语句中的 `or` 连接并不是共享左侧的 `char ==`，由于非空字符串 `'e'` 永远为真 (Truthy)，该条件将永远返回 True。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_in_operator_sequence",
        "description": "掌握多条件匹配的正确语法，应当使用 `in` 关键字，如 `char in 'aeiou'`，或者将每个条件完整写出。",
        "prerequisites": ["kc_or_operator_truthy"]
      }
    ]
  },
  {
    "id": "task_026_multi_debug",
    "problem_description": "编写一个函数 `find_max_value(nums)`，找到列表中的最大值，并返回字符串 \"Max is: [最大值]\"。",
    "buggy_code": "def find_max_value(nums):\n    max = 0\n    max = max(nums)\n    return f\"Max is: {max}\"",
    "kcs": [
      {
        "kc_id": "kc_shadowing_builtin",
        "description": "理解给变量命名为 `max` 会遮蔽 (Shadowing) Python 内置的 `max()` 函数，使其失去原有功能。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_typeerror_not_callable",
        "description": "理解在遮蔽后，`max` 变成了一个整数 `0`，紧接着调用 `max(nums)` 会触发 TypeError: 'int' object is not callable。",
        "prerequisites": ["kc_shadowing_builtin"]
      }
    ]
  },
  {
    "id": "task_027_multi_debug",
    "problem_description": "编写一个函数 `get_last_half(lst)`，使用 while 循环返回列表的后半部分元素。",
    "buggy_code": "def get_last_half(lst):\n    res = []\n    i = len(lst) // 2\n    while i <= len(lst):\n        res.append(lst[i])\n        i += 1\n    return res",
    "kcs": [
      {
        "kc_id": "kc_list_index_out_of_bounds",
        "description": "理解列表的有效索引范围是 `0` 到 `len(lst) - 1`。当 `i` 等于 `len(lst)` 时，`lst[i]` 会触发 IndexError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_while_loop_boundary",
        "description": "掌握循环边界条件的修正，必须将 while 循环条件改为严于界限的 `i < len(lst)`。",
        "prerequisites": ["kc_list_index_out_of_bounds"]
      }
    ]
  },
  {
    "id": "task_028_multi_debug",
    "problem_description": "编写一个函数 `find_pair_sum(lst1, lst2, target)`，寻找两个列表中相加等于目标值的第一个数对，并以元组 `(x, y)` 形式返回。如果没找到返回 None。",
    "buggy_code": "def find_pair_sum(lst1, lst2, target):\n    for x in lst1:\n        for y in lst2:\n            if x + y == target:\n                break\n    return (x, y)",
    "kcs": [
      {
        "kc_id": "kc_break_inner_loop",
        "description": "理解 `break` 语句只能跳出当前所在的最内层循环。外层循环 `x` 将继续执行，导致返回结果必定是列表最后的组合。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_return_early",
        "description": "掌握替代双层循环 break 的做法：直接在判断成功时使用 `return (x, y)` 提前终止整个函数逻辑。",
        "prerequisites": ["kc_break_inner_loop"]
      }
    ]
  },
  {
    "id": "task_029_multi_debug",
    "problem_description": "编写一个函数 `check_float_sum(a, b, expected)`，检查 a 和 b 的和是否等于 expected。由于输入是浮点数（例如 a=0.1, b=0.2, expected=0.3），请确保检查安全。",
    "buggy_code": "def check_float_sum(a, b, expected):\n    return a + b == expected",
    "kcs": [
      {
        "kc_id": "kc_float_precision_issue",
        "description": "理解浮点数在计算机底层的二进制表示存在精度丢失问题，例如 0.1 + 0.2 实际得到 0.30000000000000004，直接使用 `==` 会返回 False。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_math_isclose",
        "description": "掌握浮点数比较的正确方式，应使用绝对差值 `abs((a + b) - expected) < 1e-9` 或 Python 标准库 `math.isclose()`。",
        "prerequisites": ["kc_float_precision_issue"]
      }
    ]
  },
  {
    "id": "task_030_multi_debug",
    "problem_description": "编写一个函数 `create_multipliers(n)`，返回一个包含 n 个函数的列表，列表中第 i 个函数接收一个输入并将其乘以 i。",
    "buggy_code": "def create_multipliers(n):\n    funcs = []\n    for i in range(n):\n        funcs.append(lambda x: x * i)\n    return funcs",
    "kcs": [
      {
        "kc_id": "kc_closure_late_binding",
        "description": "理解 Python 中闭包的延迟绑定特性 (Late Binding)。Lambda 内部的 `i` 在循环结束时才会实际求值，导致所有函数都会乘以最终的 i 值。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_default_arg_early_binding",
        "description": "掌握如何打破延迟绑定：通过为 lambda 添加默认参数 `lambda x, i=i: x * i`，在创建函数时立即捕获当前循环中 i 的值。",
        "prerequisites": ["kc_closure_late_binding"]
      }
    ]
  },
  {
    "id": "task_031_multi_debug",
    "problem_description": "编写一个函数 `remove_negatives(d)`，接收一个字典。如果字典中的值小于 0，则将该键值对从字典中删除。返回修改后的字典。",
    "buggy_code": "def remove_negatives(d):\n    for k, v in d.items():\n        if v < 0:\n            del d[k]\n    return d",
    "kcs": [
      {
        "kc_id": "kc_dict_size_change_iteration",
        "description": "理解在 Python 中，不能在遍历字典（或其 items/keys/values）的同时对其进行增删操作，这会触发 RuntimeError: dictionary changed size during iteration。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_iterate_dict_copy",
        "description": "掌握安全的删除策略：使用 `list(d.keys())` 包装为静态列表进行遍历，或者使用字典推导式 `{k: v for k, v in d.items() if v >= 0}` 创建新字典。",
        "prerequisites": ["kc_dict_size_change_iteration"]
      }
    ]
  },
  {
    "id": "task_032_multi_debug",
    "problem_description": "编写一个函数 `get_sorted_reverse(lst)`，接收一个数字列表，将其降序排序后返回一个全新的列表，原列表不能被修改。",
    "buggy_code": "def get_sorted_reverse(lst):\n    res = lst.sort()\n    return res[::-1]",
    "kcs": [
      {
        "kc_id": "kc_list_sort_inplace_none",
        "description": "理解列表的 `.sort()` 方法是就地 (in-place) 排序，它的返回值永远是 `None`，而不是排序后的列表。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_nonetype_subscriptable",
        "description": "预见对 `res` (此时为 None) 执行切片 `[::-1]` 会触发 TypeError: 'NoneType' object is not subscriptable。应改用内置函数 `sorted(lst, reverse=True)`。",
        "prerequisites": ["kc_list_sort_inplace_none"]
      }
    ]
  },
  {
    "id": "task_033_multi_debug",
    "problem_description": "编写一个函数 `create_3x3_matrix()`，返回一个 3x3 的二维数组，所有元素初始化为 0。然后把中心点（第二行第二列）的值修改为 1。",
    "buggy_code": "def create_3x3_matrix():\n    matrix = [[0] * 3] * 3\n    matrix[1][1] = 1\n    return matrix",
    "kcs": [
      {
        "kc_id": "kc_list_multiplication_reference",
        "description": "理解使用 `[x] * 3` 乘法语法创建二维数组时，外层列表包含的是对同一个内部列表 `[0, 0, 0]` 的三次浅拷贝引用。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_aliasing_mutation",
        "description": "理解由于三个元素指向同一内存地址，修改 `matrix[1][1]` 会导致所有行的第二列全变成 1。必须使用列表推导式 `[[0] * 3 for _ in range(3)]` 来创建独立的行。",
        "prerequisites": ["kc_list_multiplication_reference"]
      }
    ]
  },
  {
    "id": "task_034_multi_debug",
    "problem_description": "编写一个函数 `calculate_avg(nums)`。使用生成器表达式筛选出所有大于 0 的数字，然后计算它们的平均值。如果全小于等于0，返回0。",
    "buggy_code": "def calculate_avg(nums):\n    positives = (x for x in nums if x > 0)\n    count = sum(1 for _ in positives)\n    if count == 0:\n        return 0\n    return sum(positives) / count",
    "kcs": [
      {
        "kc_id": "kc_generator_exhaustion",
        "description": "理解生成器 (Generator) 是一种单向迭代器。在计算 `count` 时，`positives` 已经被遍历到尽头（耗尽）。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_generator_empty_sum",
        "description": "预见由于生成器已耗尽，第二次调用 `sum(positives)` 时会得到 0，导致平均值计算永远返回 0.0。必须在需要多次遍历时先将其转换为列表 `list(...)`。",
        "prerequisites": ["kc_generator_exhaustion"]
      }
    ]
  },
  {
    "id": "task_035_multi_debug",
    "problem_description": "编写一个函数 `compare_lists(lst1, lst2)`，判断两个列表的内容是否完全相同。如果是返回 True，否则返回 False。",
    "buggy_code": "def compare_lists(lst1, lst2):\n    if lst1 is lst2:\n        return True\n    return False",
    "kcs": [
      {
        "kc_id": "kc_is_vs_equals",
        "description": "理解 `is` 运算符比较的是两个对象的内存地址（身份标识 Identity），而不是它们的值。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_value_equality",
        "description": "理解即使 `[1, 2]` 和 `[1, 2]` 内容相同，如果在不同地方创建，它们的地址也不同，`is` 会返回 False。必须使用双等号 `==` 来进行值比较 (Value Equality)。",
        "prerequisites": ["kc_is_vs_equals"]
      }
    ]
  },
  {
    "id": "task_036_multi_debug",
    "problem_description": "编写一个函数 `process_two_queues()`。初始化两个空列表 `q1` 和 `q2`。向 `q1` 中加入数字 1，向 `q2` 中加入数字 2。最终返回包含这两个列表的元组。",
    "buggy_code": "def process_two_queues():\n    q1 = q2 = []\n    q1.append(1)\n    q2.append(2)\n    return (q1, q2)",
    "kcs": [
      {
        "kc_id": "kc_chained_assignment_mutation",
        "description": "理解链式赋值 `q1 = q2 = []` 会将 `q1` 和 `q2` 绑定到内存中的同一个列表对象上。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_independent_initialization",
        "description": "理解由于引用同一对象，向 `q1` 和 `q2` 追加元素实际上都在修改同一个列表，最终两者都会变成 `[1, 2]`。必须拆开初始化：`q1, q2 = [], []`。",
        "prerequisites": ["kc_chained_assignment_mutation"]
      }
    ]
  },
  {
    "id": "task_037_multi_debug",
    "problem_description": "编写一个函数 `is_palindrome(word)`，判断给定的单词是否为回文（正着读和反着读一样）。",
    "buggy_code": "def is_palindrome(word):\n    reversed_word = reversed(word)\n    if word == reversed_word:\n        return True\n    return False",
    "kcs": [
      {
        "kc_id": "kc_reversed_iterator",
        "description": "理解内置函数 `reversed()` 并不直接返回倒序后的字符串或列表，而是返回一个迭代器对象 (Iterator object)。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_compare_str_iterator",
        "description": "理解字符串直接和迭代器对象使用 `==` 比较，结果永远是 False。必须使用 `\"\".join(reversed(word))` 或切片法 `word[::-1]` 将其还原为字符串再比较。",
        "prerequisites": ["kc_reversed_iterator"]
      }
    ]
  },
  {
    "id": "task_038_multi_debug",
    "problem_description": "编写一个函数 `divide_safe(a, b)`，尝试计算 `a / b` 并返回结果。如果抛出异常，应该返回 -1。最后无论是否异常，都在控制台打印 'Done' (用返回 0 模拟清理工作)。",
    "buggy_code": "def divide_safe(a, b):\n    try:\n        return a / b\n    except Exception:\n        return -1\n    finally:\n        return 0",
    "kcs": [
      {
        "kc_id": "kc_finally_execution_guarantee",
        "description": "理解 `finally` 块的特殊性：无论 `try` 或 `except` 块中是否发生了 `return`，`finally` 块中的代码都必定会被执行。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_finally_return_override",
        "description": "理解如果在 `finally` 块中执行了 `return` 语句，它会无情地覆盖掉 `try` 或 `except` 中的返回值。导致该函数永远返回 0。应当移除 finally 中的 return。",
        "prerequisites": ["kc_finally_execution_guarantee"]
      }
    ]
  },
  {
    "id": "task_039_multi_debug",
    "problem_description": "编写一个函数 `dot_product(l1, l2)`，计算两个列表的点积。要求：如果两个列表长度不一致，必须抛出 ValueError。目前的逻辑有漏洞。",
    "buggy_code": "def dot_product(l1, l2):\n    return sum(a * b for a, b in zip(l1, l2))",
    "kcs": [
      {
        "kc_id": "kc_zip_truncation",
        "description": "理解内置函数 `zip()` 在合并多个可迭代对象时，如果长度不一，会以最短的那个为准，多余的元素会被静默丢弃 (Silently truncated)。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_explicit_length_check",
        "description": "理解因为静默丢弃的存在，代码不会自动报错。必须在调用 `zip` 之前显式添加守卫语句 `if len(l1) != len(l2): raise ValueError`（或在 Python 3.10+ 使用 strict=True）。",
        "prerequisites": ["kc_zip_truncation"]
      }
    ]
  },
  {
    "id": "task_040_multi_debug",
    "problem_description": "编写一个函数 `parse_boolean(s)`，接收一个字符串并将其转为布尔值。如果输入是 \"False\" (无论大小写)，则返回 False，否则默认返回 True。",
    "buggy_code": "def parse_boolean(s):\n    if bool(s) == False:\n        return False\n    return True",
    "kcs": [
      {
        "kc_id": "kc_bool_cast_nonempty_string",
        "description": "理解在 Python 中，`bool()` 函数对字符串的转换规则是：空字符串 `\"\"` 为 False，任何非空字符串（包括 `\"False\"`）统统为 True。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_string_literal_comparison",
        "description": "预见由于 `bool(\"False\")` 结果为 True，这段代码对 \"False\" 输入无法按预期返回 False。必须改用纯字符串匹配逻辑 `s.lower() == \"false\"`。",
        "prerequisites": ["kc_bool_cast_nonempty_string"]
      }
    ]
  },
  {
    "id": "task_041_multi_debug",
    "problem_description": "编写一个函数 `count_frequency(data_list)`，接收一个列表，列表中的元素可能是另一个列表（例如 `[1, [1, 2], 1]`）。函数需要统计每个元素出现的次数并返回字典。",
    "buggy_code": "def count_frequency(data_list):\n    freq = {}\n    for item in data_list:\n        freq[item] = freq.get(item, 0) + 1\n    return freq",
    "kcs": [
      {
        "kc_id": "kc_unhashable_type_list",
        "description": "理解列表 (List) 是可变对象，属于不可哈希类型 (Unhashable type)，不能直接作为字典的键 (Key)。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_tuple_conversion_for_hash",
        "description": "掌握将可变序列作为字典键的方法：在存入字典前，将其转换为不可变的元组类型，例如使用 `tuple(item) if isinstance(item, list) else item`。",
        "prerequisites": ["kc_unhashable_type_list"]
      }
    ]
  },
  {
    "id": "task_042_multi_debug",
    "problem_description": "编写一个函数 `get_element_index(lst, target)`。在列表中查找目标元素的索引并返回。如果元素不存在，请安全地返回 -1。",
    "buggy_code": "def get_element_index(lst, target):\n    idx = lst.index(target)\n    if idx:\n        return idx\n    return -1",
    "kcs": [
      {
        "kc_id": "kc_list_index_valueerror",
        "description": "理解当目标元素不在列表中时，`list.index()` 方法不会返回 None 或 -1，而是直接抛出 ValueError 异常中止程序。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_try_except_valueerror",
        "description": "掌握安全的查找方式：使用 `try...except ValueError` 块包裹查找逻辑，或者在调用 `index()` 前先用 `if target in lst:` 进行存在性检查。",
        "prerequisites": ["kc_list_index_valueerror"]
      }
    ]
  },
  {
    "id": "task_043_multi_debug",
    "problem_description": "编写一个函数 `analyze_numbers(str_nums)`，接收包含数字的字符串列表（如 `['1', '2', '3']`）。要求计算这些数字的总和与最大值，并以元组 `(sum, max)` 形式返回。",
    "buggy_code": "def analyze_numbers(str_nums):\n    nums = map(int, str_nums)\n    total = sum(nums)\n    maximum = max(nums)\n    return (total, maximum)",
    "kcs": [
      {
        "kc_id": "kc_map_iterator_exhaustion",
        "description": "理解在 Python 3 中，`map()` 函数返回的是一个惰性求值的迭代器 (Iterator)，一旦被遍历过一次（如执行 `sum()`），它就会彻底耗尽。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_list_materialization",
        "description": "理解当后续代码（如执行 `max()`）再次访问耗尽的迭代器时，会抛出 ValueError (max() arg is an empty sequence)。必须用 `nums = list(map(int, str_nums))` 提前将其物化为列表。",
        "prerequisites": ["kc_map_iterator_exhaustion"]
      }
    ]
  },
  {
    "id": "task_044_multi_debug",
    "problem_description": "编写一个函数 `get_user_greeting(user_dict)`，接收一个字典（包含 'name' 键）。使用 f-string 返回问候语：\"Hello, [name]!\"。",
    "buggy_code": "def get_user_greeting(user_dict):\n    return f\"Hello, {user_dict[\"name\"]}!\"",
    "kcs": [
      {
        "kc_id": "kc_fstring_quote_conflict",
        "description": "理解在 f-string 的花括号内直接使用与外层相同的双引号 `\"` 会导致字符串解析器过早闭合，引发 SyntaxError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_fstring_quote_alternation",
        "description": "掌握解决引号冲突的规范方案：在外层使用双引号时，字典键的访问必须使用单引号（如 `{user_dict['name']}`），或反之亦然。",
        "prerequisites": ["kc_fstring_quote_conflict"]
      }
    ]
  },
  {
    "id": "task_045_multi_debug",
    "problem_description": "编写一个函数 `round_half_up(num)`，实现传统的“四舍五入”（即逢 0.5 永远进位）。例如输入 2.5 返回 3，输入 3.5 返回 4。",
    "buggy_code": "def round_half_up(num):\n    return round(num)",
    "kcs": [
      {
        "kc_id": "kc_bankers_rounding",
        "description": "理解 Python 3 的 `round()` 函数采用的是“银行家舍入法”（Banker's Rounding），即遇到 0.5 时会向最近的【偶数】舍入（2.5 变 2，3.5 会变成 4）。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_math_floor_addition",
        "description": "掌握实现传统四舍五入的正确数学逻辑：可以使用 `math.floor(num + 0.5)`，或者在 decimal 模块中显式指定 ROUND_HALF_UP 策略。",
        "prerequisites": ["kc_bankers_rounding"]
      }
    ]
  },
  {
    "id": "task_046_multi_debug",
    "problem_description": "编写一个函数 `fibonacci_next(a, b)`，接收斐波那契数列当前的两个相邻数字，并推进到下一个状态，返回更新后的 `(a, b)`，其中新的 a 是旧的 b，新的 b 是两者的和。",
    "buggy_code": "def fibonacci_next(a, b):\n    a = b\n    b = a + b\n    return (a, b)",
    "kcs": [
      {
        "kc_id": "kc_sequential_assignment_overwrite",
        "description": "理解顺序执行 `a = b` 会立即覆盖变量 `a` 的原值。导致紧接着的 `b = a + b` 实际上变成了 `b = b + b`，计算逻辑完全崩溃。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_tuple_unpacking",
        "description": "掌握 Python 中元组解包 (Tuple Unpacking) 的特性，使用 `a, b = b, a + b` 可以确保等号右侧在任何变量被覆盖前完成所有计算并同时赋值。",
        "prerequisites": ["kc_sequential_assignment_overwrite"]
      }
    ]
  },
  {
    "id": "task_047_multi_debug",
    "problem_description": "编写一个函数 `make_counter()`，返回一个内部闭包函数。每次调用该闭包函数，内部计数器加 1 并返回当前计数。",
    "buggy_code": "def make_counter():\n    count = 0\n    def increment():\n        count += 1\n        return count\n    return increment",
    "kcs": [
      {
        "kc_id": "kc_unbound_local_error_closure",
        "description": "理解在内部函数 `increment` 中执行 `count += 1` 时，Python 会将其视为创建并修改局部变量，但在赋值前读取外层 `count` 会触发 UnboundLocalError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_nonlocal_keyword",
        "description": "掌握如何跨作用域修改外部闭包变量：必须在内部函数首行显式声明 `nonlocal count`，告知解释器该变量属于外层函数作用域。",
        "prerequisites": ["kc_unbound_local_error_closure"]
      }
    ]
  },
  {
    "id": "task_048_multi_debug",
    "problem_description": "编写一个函数 `has_critical_errors(errors)`。如果错误列表中的**所有**错误都是致命的（即 `e.is_critical == True`），则返回 True。注意：如果没有错误（空列表），说明系统处于健康状态，应返回 False。",
    "buggy_code": "def has_critical_errors(errors):\n    return all(e.is_critical for e in errors)",
    "kcs": [
      {
        "kc_id": "kc_vacuous_truth_all",
        "description": "理解 `all()` 函数的“空洞真值”（Vacuous Truth）特性：当传入空的可迭代对象时，`all([])` 会无条件返回 True，这违背了题目的健康状态定义。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_boolean_short_circuit_guard",
        "description": "掌握防范空容器导致 `all()` 逻辑谬误的方法：添加显式的容器非空检查，如使用短路逻辑 `bool(errors) and all(...)` 兜底。",
        "prerequisites": ["kc_vacuous_truth_all"]
      }
    ]
  },
  {
    "id": "task_049_multi_debug",
    "problem_description": "编写一个函数 `write_log(msg)`，向当前目录的 'log.txt' 追加写入一行日志，并返回 True。",
    "buggy_code": "def write_log(msg):\n    f = open('log.txt', 'a')\n    f.write(msg + '\\n')\n    return True",
    "kcs": [
      {
        "kc_id": "kc_file_buffer_flush",
        "description": "理解文件写入操作通常会被操作系统缓冲在内存中。如果函数返回前没有调用 `f.close()` 或 `f.flush()`，可能导致数据丢失或延迟落盘。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_context_manager_with",
        "description": "掌握 Python 文件操作的最佳实践：使用 `with open(...) as f:` 上下文管理器，无论后续代码是否抛出异常或直接 return，系统都会安全且自动地关闭文件句柄。",
        "prerequisites": ["kc_file_buffer_flush"]
      }
    ]
  },
  {
    "id": "task_050_multi_debug",
    "problem_description": "编写一个函数 `safe_convert_int(s)`。尝试将输入转换为整数。如果遇到值错误（如 'abc'）或类型错误（如列表），都捕获并返回 0。",
    "buggy_code": "def safe_convert_int(s):\n    try:\n        return int(s)\n    except TypeError or ValueError:\n        return 0",
    "kcs": [
      {
        "kc_id": "kc_exception_or_evaluation",
        "description": "理解在 except 子句中，表达式 `TypeError or ValueError` 会被立即求值为其第一个真值，即 `TypeError` 对象本身，从而使得 ValueError 完全被漏捕获。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_exception_tuple_syntax",
        "description": "掌握在单个 except 块中捕获多种异常的正确语法：必须将目标异常类包装在一个元组中，写为 `except (TypeError, ValueError):`。",
        "prerequisites": ["kc_exception_or_evaluation"]
      }
    ]
  },
  {
    "id": "task_051_multi_debug",
    "problem_description": "编写一个函数 `find_plural_words(text)`，使用正则表达式提取出文本中所有以字母 's' 结尾的单词。注意单词边界。",
    "buggy_code": "import re\ndef find_plural_words(text):\n    return re.findall('\\b\\w+s\\b', text)",
    "kcs": [
      {
        "kc_id": "kc_regex_escape_sequence",
        "description": "理解在 Python 普通字符串中，`\\b` 会被转义为退格符 (Backspace)，而不是正则表达式中的“单词边界”，导致正则匹配彻底失效。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_raw_string_prefix",
        "description": "掌握解决转义冲突的最佳实践：在正则表达式字符串前加上 `r` 前缀（如 `r'\\b\\w+s\\b'`），声明其为原始字符串 (Raw string)。",
        "prerequisites": ["kc_regex_escape_sequence"]
      }
    ]
  },
  {
    "id": "task_052_multi_debug",
    "problem_description": "编写一个函数 `validate_age(age)`，使用 assert 语句断言年龄不能为负数，如果小于 0 应触发 AssertionError 并在报错信息中显示 'Age cannot be negative'。",
    "buggy_code": "def validate_age(age):\n    assert (age >= 0, \"Age cannot be negative\")\n    return True",
    "kcs": [
      {
        "kc_id": "kc_assert_tuple_truthy",
        "description": "理解 `assert (condition, msg)` 这种带括号的写法实际上是断言了一个包含两个元素的元组。在 Python 中，非空元组永远为真 (Truthy)，导致该断言永远不会失败。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_assert_syntax_comma",
        "description": "掌握 assert 的正确语法格式：它是一个关键字语句而非函数，不应使用括号包裹两个参数，必须写为 `assert age >= 0, \"Age cannot be negative\"`。",
        "prerequisites": ["kc_assert_tuple_truthy"]
      }
    ]
  },
  {
    "id": "task_053_multi_debug",
    "problem_description": "定义一个父类 `User` 和一个子类 `Admin`。父类的初始化方法中定义了 `self.is_active = True`。子类 Admin 在初始化时不仅需要拥有父类的属性，还要新增一个 `self.role = 'admin'`。目前的子类代码有问题。",
    "buggy_code": "class User:\n    def __init__(self):\n        self.is_active = True\n\nclass Admin(User):\n    def __init__(self):\n        self.role = 'admin'",
    "kcs": [
      {
        "kc_id": "kc_subclass_init_override",
        "description": "理解在子类中重写 (Override) `__init__` 方法会完全屏蔽父类的 `__init__`，导致 `self.is_active` 永远不会被初始化。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_super_init_call",
        "description": "掌握在子类构造函数中显式调用父类状态初始化的机制：必须在子类的 `__init__` 中添加 `super().__init__()`。",
        "prerequisites": ["kc_subclass_init_override"]
      }
    ]
  },
  {
    "id": "task_054_multi_debug",
    "problem_description": "编写一个函数 `group_by_length(words)`，接收一个单词列表，使用 `collections.defaultdict` 按单词长度进行分组，返回一个字典。例如 `{3: ['cat', 'dog']}`。",
    "buggy_code": "from collections import defaultdict\n\ndef group_by_length(words):\n    d = defaultdict()\n    for w in words:\n        d[len(w)].append(w)\n    return d",
    "kcs": [
      {
        "kc_id": "kc_defaultdict_missing_factory",
        "description": "理解 `defaultdict` 在实例化时，必须传入一个不带参数的可调用对象 (Callable) 作为 `default_factory`，空参数会导致它退化为普通字典并抛出 KeyError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_defaultdict_list_factory",
        "description": "掌握修复工厂函数的方法：传入 `list` 类的引用（即 `defaultdict(list)`），这样在遇到未知键时会自动实例化一个空列表以供 `append`。",
        "prerequisites": ["kc_defaultdict_missing_factory"]
      }
    ]
  },
  {
    "id": "task_055_multi_debug",
    "problem_description": "编写一个函数 `split_into_chars(word)`，接收一个字符串并将其拆分为单个字符的列表。例如输入 \"cat\"，返回 `['c', 'a', 't']`。",
    "buggy_code": "def split_into_chars(word):\n    return word.split(\"\")",
    "kcs": [
      {
        "kc_id": "kc_split_empty_separator",
        "description": "理解 Python 的 `str.split(sep)` 方法不支持空字符串 `\"\"` 作为分隔符，执行 `split(\"\")` 会直接抛出 ValueError: empty separator。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_list_constructor_for_chars",
        "description": "掌握将字符串转换为字符列表的内置且最优雅的方式：直接调用列表构造器 `list(word)`。",
        "prerequisites": ["kc_split_empty_separator"]
      }
    ]
  },
  {
    "id": "task_056_multi_debug",
    "problem_description": "自定义一个队列类 `MyQueue`。编写一个函数 `process_queue(q)`，如果队列非空，则返回 \"Processing\"，如果队列为空，则返回 \"Empty\"。",
    "buggy_code": "class MyQueue:\n    def __init__(self):\n        self.items = []\n\ndef process_queue(q):\n    if q:\n        return \"Processing\"\n    return \"Empty\"",
    "kcs": [
      {
        "kc_id": "kc_custom_object_truthiness",
        "description": "理解自定义类的实例对象默认总是被判定为 True (Truthy)。因此即使 `q.items` 为空，`if q:` 也会进入 True 分支。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_magic_method_bool_or_len",
        "description": "掌握让对象支持布尔求值的两种方法：在类中实现 `__bool__(self)` 或 `__len__(self)` 魔术方法，或者在条件判断时明确检查内部数据结构 `if q.items:`。",
        "prerequisites": ["kc_custom_object_truthiness"]
      }
    ]
  },
  {
    "id": "task_057_multi_debug",
    "problem_description": "编写一个函数 `flatten_matrix(matrix)`，使用 `sum()` 函数将一个二维列表（如 `[[1, 2], [3, 4]]`）展平为一个一维列表 `[1, 2, 3, 4]`。",
    "buggy_code": "def flatten_matrix(matrix):\n    return sum(matrix)",
    "kcs": [
      {
        "kc_id": "kc_sum_default_start_int",
        "description": "理解 `sum(iterable)` 存在一个默认的 `start` 参数，初始值为整数 `0`。当尝试将整数 0 与列表 `[1, 2]` 相加时，会触发 TypeError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_sum_start_argument_list",
        "description": "掌握覆盖 `start` 参数的方法：将初始值设为一个空列表 `[]`（即 `sum(matrix, [])`），从而让 `sum` 执行列表的级联拼接操作。",
        "prerequisites": ["kc_sum_default_start_int"]
      }
    ]
  },
  {
    "id": "task_058_multi_debug",
    "problem_description": "编写一个函数 `swap_dict_kv(d)`，接收一个字典，返回一个键值对翻转的新字典（假设原字典的值也是唯一的）。",
    "buggy_code": "def swap_dict_kv(d):\n    res = {}\n    for k, v in d:\n        res[v] = k\n    return res",
    "kcs": [
      {
        "kc_id": "kc_dict_default_iteration_keys",
        "description": "理解对字典对象直接进行迭代（`for x in d:`）只会产生字典的【键(Key)】。如果键不是包含两个元素的元组，直接解包到 `k, v` 会抛出 ValueError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_dict_items_method",
        "description": "掌握同时获取键和值的正确做法：必须调用 `d.items()` 方法来生成包含 `(key, value)` 元组的迭代器。",
        "prerequisites": ["kc_dict_default_iteration_keys"]
      }
    ]
  },
  {
    "id": "task_059_multi_debug",
    "problem_description": "编写一个函数 `check_if_found(items, target)`。在列表中查找目标，如果遍历结束还没找到且最后一个检查的元素不是目标，则返回 False。注意输入列表可能为空。",
    "buggy_code": "def check_if_found(items, target):\n    for i in items:\n        if i == target:\n            break\n    if i != target:\n        return False\n    return True",
    "kcs": [
      {
        "kc_id": "kc_loop_variable_unbound_empty",
        "description": "理解如果 `items` 是一个空列表，`for` 循环体一次也不会执行。此时在循环外部尝试读取 `i` 会抛出 UnboundLocalError: local variable 'i' referenced before assignment。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_for_else_construct",
        "description": "掌握更安全的控制流设计：不依赖会泄漏的循环变量，而是引入布尔状态位 `found = False`，或使用 Python 特有的 `for...else` 结构来处理未找到的情况。",
        "prerequisites": ["kc_loop_variable_unbound_empty"]
      }
    ]
  },
  {
    "id": "task_060_multi_debug",
    "problem_description": "编写一个函数 `create_backup(config)`。接收一个配置字典（可能包含嵌套列表，如 `{'ports': [80, 443]}`），返回一个深拷贝副本以供安全修改。",
    "buggy_code": "def create_backup(config):\n    new_config = config.copy()\n    return new_config",
    "kcs": [
      {
        "kc_id": "kc_shallow_copy_nested_refs",
        "description": "理解字典自带的 `.copy()` 方法只执行【浅拷贝】（Shallow Copy）。顶层字典是新的，但内部的列表对象依然与原字典共享同一块内存地址。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_deepcopy_import",
        "description": "掌握实现深拷贝的标准方法：必须导入内置模块 `import copy`，并使用 `copy.deepcopy(config)` 来递归切断所有嵌套引用。",
        "prerequisites": ["kc_shallow_copy_nested_refs"]
      }
    ]
  },
  {
    "id": "task_061_multi_debug",
    "problem_description": "编写一个函数 `append_to_all(lists, item)`，接收一个列表的列表，向每个子列表中追加给定的元素，并返回修改后的“包含子列表的完整列表”。",
    "buggy_code": "def append_to_all(lists, item):\n    return [lst.append(item) for lst in lists]",
    "kcs": [
      {
        "kc_id": "kc_append_return_none",
        "description": "理解列表的 `.append()` 方法是原地修改列表的操作 (In-place mutation)，它的返回值永远是 `None`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_list_comp_collects_returns",
        "description": "理解列表推导式会收集每个表达式的返回值，因此上述代码不仅不会返回修改后的列表，反而会返回一个全是 None 的列表 `[None, None, ...]`。应当使用普通 for 循环后在外部 `return lists`。",
        "prerequisites": ["kc_append_return_none"]
      }
    ]
  },
  {
    "id": "task_062_multi_debug",
    "problem_description": "编写一个函数 `merge_configs(base, custom)`，将 custom 字典合并到 base 中（覆盖同名键），并返回合并后的字典。",
    "buggy_code": "def merge_configs(base, custom):\n    return base.update(custom)",
    "kcs": [
      {
        "kc_id": "kc_dict_update_inplace",
        "description": "理解字典的 `.update()` 方法同样是原地操作，它直接修改原字典，并返回 `None`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_separate_mutation_return",
        "description": "掌握修复方法：必须将更新操作和返回拆分为两步（先 `base.update(custom)`，再 `return base`），或者在 Python 3.9+ 中使用字典合并运算符 `base | custom`。",
        "prerequisites": ["kc_dict_update_inplace"]
      }
    ]
  },
  {
    "id": "task_063_multi_debug",
    "problem_description": "定义了函数 `get_point()` 返回一个二维坐标。在 `process_point()` 中调用它，并试图将坐标解包为 x, y, z 三个变量并求和。",
    "buggy_code": "def get_point():\n    return 10, 20\n\ndef process_point():\n    x, y, z = get_point()\n    return x + y + z",
    "kcs": [
      {
        "kc_id": "kc_implicit_tuple_return",
        "description": "理解在 Python 中，以逗号分隔返回多个值时（如 `return 10, 20`），实际上隐式地打包并返回了一个包含两个元素的元组 `(10, 20)`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_unpacking_mismatch",
        "description": "理解尝试将 2 个元素的元组强行解包给 3 个变量（`x, y, z`）会触发 ValueError: not enough values to unpack。两边的数量必须严格匹配。",
        "prerequisites": ["kc_implicit_tuple_return"]
      }
    ]
  },
  {
    "id": "task_064_multi_debug",
    "problem_description": "编写一个函数 `remove_https(url)`。如果 url 以 \"https://\" 开头，则将其剥离并返回剩余部分（例如 \"https://hello.com\" 变为 \"hello.com\"）。",
    "buggy_code": "def remove_https(url):\n    return url.lstrip(\"https://\")",
    "kcs": [
      {
        "kc_id": "kc_lstrip_character_set",
        "description": "理解 `lstrip(chars)` 函数将参数视为一个“字符集合”而不是固定前缀。它会剥离左侧任何出现在集合 `h, t, p, s, :, /` 中的字符，导致 \"https://hello.com\" 会错误地变成 \"ello.com\"（'h' 也被当做目标删除了）。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_removeprefix_method",
        "description": "掌握剥离固定前缀的正确方法：使用 Python 3.9 引入的 `url.removeprefix(\"https://\")`，或者采用切片逻辑 `url[len(\"https://\"):]`。",
        "prerequisites": ["kc_lstrip_character_set"]
      }
    ]
  },
  {
    "id": "task_065_multi_debug",
    "problem_description": "编写一个函数 `capitalize_first(word)`，将传入的字符串的第一个字母改为大写，并返回该字符串。假设传入的字符串不为空。",
    "buggy_code": "def capitalize_first(word):\n    word[0] = word[0].upper()\n    return word",
    "kcs": [
      {
        "kc_id": "kc_string_immutability_assignment",
        "description": "理解在 Python 中，字符串 (str) 属于不可变类型 (Immutable)。试图通过索引直接赋值（如 `word[0] = ...`）会触发 TypeError: 'str' object does not support item assignment。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_string_slicing_reconstruction",
        "description": "掌握重构不可变字符串的正确姿势：必须创建一个新字符串，例如通过切片拼接 `word[0].upper() + word[1:]`，或者直接调用内置方法 `word.capitalize()`。",
        "prerequisites": ["kc_string_immutability_assignment"]
      }
    ]
  },
  {
    "id": "task_066_multi_debug",
    "problem_description": "编写一个函数 `log_error()`。尝试执行 `1 / 0`，如果发生异常，捕获该异常对象并将其赋值给外部变量 `err`，最后在函数末尾返回该错误对象。",
    "buggy_code": "def log_error():\n    err = None\n    try:\n        1 / 0\n    except ZeroDivisionError as err:\n        pass\n    return err",
    "kcs": [
      {
        "kc_id": "kc_except_variable_deletion",
        "description": "理解 Python 3 中的机制怪癖：在 `except ... as err:` 块结束时，异常变量 `err` 会被解释器隐式地强制 `del` 删除，以避免循环引用。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_unbound_local_after_except",
        "description": "预见由于 `err` 在块结束后被销毁，导致外部初始化的 `err` 也荡然无存。执行 `return err` 会触发 UnboundLocalError。必须在块内将其赋值给另一个不受 `as` 约束的别名变量。",
        "prerequisites": ["kc_except_variable_deletion"]
      }
    ]
  },
  {
    "id": "task_067_multi_debug",
    "problem_description": "编写一个函数 `countdown(n)`，返回一个从 n 倒数到 1 的列表。例如输入 5，返回 `[5, 4, 3, 2, 1]`。",
    "buggy_code": "def countdown(n):\n    return list(range(n, 0))",
    "kcs": [
      {
        "kc_id": "kc_range_default_step",
        "description": "理解 `range(start, stop)` 的默认步长 (step) 为正数 1。试图以步长 1 从大数数到小数（如从 5 到 0），会直接产生并返回一个空的区间序列。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_range_negative_step",
        "description": "掌握倒序生成序列的语法规则：必须显式传入第三个参数 `step=-1`，即写为 `range(n, 0, -1)` 才能正确倒数。",
        "prerequisites": ["kc_range_default_step"]
      }
    ]
  },
  {
    "id": "task_068_multi_debug",
    "problem_description": "编写一个函数 `get_unique(nums)`，使用集合 (Set) 去除列表中的重复数字，并返回该集合。",
    "buggy_code": "def get_unique(nums):\n    s = {}\n    for n in nums:\n        s.add(n)\n    return s",
    "kcs": [
      {
        "kc_id": "kc_empty_dict_literal",
        "description": "理解在 Python 中，字面量 `{}` 优先用于字典的定义。因此它创建的是一个空字典 (dict)，而不是空集合 (set)。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_dict_no_add_method",
        "description": "理解字典对象没有 `.add()` 方法，执行 `s.add(n)` 会抛出 AttributeError。必须显式调用工厂函数 `s = set()` 来初始化空集合。",
        "prerequisites": ["kc_empty_dict_literal"]
      }
    ]
  },
  {
    "id": "task_069_multi_debug",
    "problem_description": "编写一个函数 `floor_divide(a, b)`，实现数学意义上的向下取整除法（向负无穷方向截断）。注意需要正确处理负数，例如 `floor_divide(-3, 2)` 应该返回 `-2`。",
    "buggy_code": "def floor_divide(a, b):\n    return int(a / b)",
    "kcs": [
      {
        "kc_id": "kc_int_truncation_towards_zero",
        "description": "理解 `/` 产生浮点数（如 -1.5），而 `int()` 函数在转换浮点数时是“向零截断”（Truncate towards zero），所以 `int(-1.5)` 会变成 `-1`，这不符合向下取整的数学定义。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_floor_division_operator",
        "description": "掌握 Python 中专门用于数学向下取整的底除法 (Floor Division) 运算符：应当直接抛弃 `int(a/b)`，改用整除符号 `a // b`。",
        "prerequisites": ["kc_int_truncation_towards_zero"]
      }
    ]
  },
  {
    "id": "task_070_multi_debug",
    "problem_description": "编写一个函数 `format_ranking(names)`，接收一个名字列表，返回带有排名的字符串列表（如 `[\"1: Alice\", \"2: Bob\"]`）。",
    "buggy_code": "def format_ranking(names):\n    res = []\n    for idx, name in enumerate(names, 1):\n        res.append(idx + \": \" + name)\n    return res",
    "kcs": [
      {
        "kc_id": "kc_enumerate_yields_int",
        "description": "理解 `enumerate(names, 1)` 解包出来的索引计数器 `idx` 是严格的整数 (int) 类型。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_typeerror_int_str_concat",
        "description": "理解 Python 属于强类型语言，不允许直接使用 `+` 运算符将整数与字符串拼接，否则会抛出 TypeError。必须用 `str(idx)` 显式转换，或使用现代的 f-string `f\"{idx}: {name}\"`。",
        "prerequisites": ["kc_enumerate_yields_int"]
      }
    ]
  },
  {
    "id": "task_071_multi_debug",
    "problem_description": "编写一个函数 `filter_integers(items)`，接收一个混合类型列表（如 `[1, \"a\", True, 2.5, 3]`），仅筛选出其中的严格整数（不包括布尔值），返回 `[1, 3]`。",
    "buggy_code": "def filter_integers(items):\n    res = []\n    for item in items:\n        if isinstance(item, int):\n            res.append(item)\n    return res",
    "kcs": [
      {
        "kc_id": "kc_bool_subclass_of_int",
        "description": "理解在 Python 中，`bool` 类型在底层是 `int` 类型的子类。因此 `isinstance(True, int)` 会返回 True，导致布尔值被错误地当作整数收集。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_strict_type_checking",
        "description": "掌握严格类型检查的正确姿势：可以额外增加排他条件 `if isinstance(item, int) and not isinstance(item, bool):`，或者使用精确类型匹配 `if type(item) is int:`。",
        "prerequisites": ["kc_bool_subclass_of_int"]
      }
    ]
  },
  {
    "id": "task_072_multi_debug",
    "problem_description": "编写一个函数 `is_prime(n)`，判断大于1的整数 n 是否为素数。要求利用 Python 的 `for...else` 结构。",
    "buggy_code": "def is_prime(n):\n    for i in range(2, n):\n        if n % i == 0:\n            break\n        else:\n            return False\n    return True",
    "kcs": [
      {
        "kc_id": "kc_for_else_indentation",
        "description": "理解当前的 `else` 错误地与 `if` 语句对齐，导致一旦 `n` 不能被当前的 `i` 整除（例如 9 不能被 2 整除），就会立刻返回 False（误判为合数）。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_for_else_semantics",
        "description": "掌握 `for...else` 的真正语义：`else` 必须与 `for` 对齐，它表示“当循环正常耗尽（没有被 break 提前中断）时才执行”。如果找到因子则 break，若循环走完都没 break，说明是素数。",
        "prerequisites": ["kc_for_else_indentation"]
      }
    ]
  },
  {
    "id": "task_073_multi_debug",
    "problem_description": "编写一个函数 `remove_even_indices(lst)`，原地删除列表中所有偶数索引（0, 2, 4...）上的元素。",
    "buggy_code": "def remove_even_indices(lst):\n    for i in range(len(lst)):\n        if i % 2 == 0:\n            del lst[i]\n    return lst",
    "kcs": [
      {
        "kc_id": "kc_list_mutation_index_shift",
        "description": "理解在正向循环中通过 `del` 删除列表元素，会导致后续元素整体左移。索引 `i` 继续递增时，不仅会跳过元素，还会引发 IndexError: list assignment index out of range。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_reverse_iteration_deletion",
        "description": "掌握通过索引安全删除元素的方案：必须倒序遍历索引 `for i in range(len(lst) - 1, -1, -1):`，这样删除操作产生的索引偏移就不会影响尚未处理的前置元素。",
        "prerequisites": ["kc_list_mutation_index_shift"]
      }
    ]
  },
  {
    "id": "task_074_multi_debug",
    "problem_description": "编写一个函数 `sort_dict_by_value(d)`，接收一个字典（键为字符串，值为整数），按字典的值进行升序排序，返回一个排序后的键值对列表。",
    "buggy_code": "def sort_dict_by_value(d):\n    return sorted(d, key=lambda x: x[1])",
    "kcs": [
      {
        "kc_id": "kc_sorted_dict_yields_keys",
        "description": "理解直接将字典传入 `sorted(d)`，实际上只对字典的【键 (Keys)】进行迭代。此时 lambda 表达式接收到的 `x` 只是一个字符串键。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_string_index_error_in_lambda",
        "description": "预见 `x[1]` 试图获取字符串的第二个字符（若字符串长度不足还会报错），根本不是在获取字典的值。必须修改为对键值对元组进行排序：`sorted(d.items(), key=lambda item: item[1])`。",
        "prerequisites": ["kc_sorted_dict_yields_keys"]
      }
    ]
  },
  {
    "id": "task_075_multi_debug",
    "problem_description": "编写一个函数 `format_ids(id_list)`，接收一个整数 ID 列表（如 `[101, 102]`），用逗号加空格拼接成字符串并返回，例如 `\"101, 102\"`。",
    "buggy_code": "def format_ids(id_list):\n    return \", \".join(id_list)",
    "kcs": [
      {
        "kc_id": "kc_join_requires_strings",
        "description": "理解字符串的 `.join(iterable)` 方法强制要求迭代器内的所有元素都必须是字符串类型。传入整数列表会抛出 TypeError: sequence item 0: expected str instance, int found。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_generator_expression_conversion",
        "description": "掌握类型转换的最佳实践：使用生成器表达式在拼接前进行映射 `\", \".join(str(i) for i in id_list)`，或者使用 `map` 函数 `\", \".join(map(str, id_list))`。",
        "prerequisites": ["kc_join_requires_strings"]
      }
    ]
  },
  {
    "id": "task_076_multi_debug",
    "problem_description": "编写一个函数 `check_cache(val)`。系统规定一个缓存阈值 1000。比较传入的值与 1000 是否完全一致，如果是返回 True。",
    "buggy_code": "def check_cache(val):\n    if val is 1000:\n        return True\n    return False",
    "kcs": [
      {
        "kc_id": "kc_is_operator_memory_identity",
        "description": "理解 `is` 运算符比较的是内存地址（身份）。CPython 解释器只对 -5 到 256 的小整数进行了全局缓存。对于 1000，不同代码块中创建的对象内存地址可能不同，导致条件极不稳定甚至报错。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_equality_operator_values",
        "description": "掌握值比较的正确规范：在比较数字、字符串等值类型时，永远使用 `==` (Value Equality) 而不是 `is`，否则会引发 SyntaxWarning 或难以复现的 Bug。",
        "prerequisites": ["kc_is_operator_memory_identity"]
      }
    ]
  },
  {
    "id": "task_077_multi_debug",
    "problem_description": "定义了函数 `calculate_volume(l, w, h)` 计算体积。编写 `get_volume(dimensions)` 接收一个包含三个维度的列表并调用该函数。",
    "buggy_code": "def calculate_volume(l, w, h):\n    return l * w * h\n\ndef get_volume(dimensions):\n    return calculate_volume(dimensions)",
    "kcs": [
      {
        "kc_id": "kc_missing_positional_args",
        "description": "理解将一个列表直接传递给函数时，它只占据了第一个位置参数 `l`。函数会因为缺少 `w` 和 `h` 两个位置参数而抛出 TypeError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_iterable_unpacking",
        "description": "掌握参数解包 (Argument Unpacking) 的语法：必须在列表前加上星号 `*dimensions`，将其解包为 3 个独立的参数依次传入。",
        "prerequisites": ["kc_missing_positional_args"]
      }
    ]
  },
  {
    "id": "task_078_multi_debug",
    "problem_description": "定义一个 `Person` 类。为了能以 `[Name]` 格式打印该对象，重写了 `__str__` 方法。编写函数 `print_people()`，创建对象放入列表并返回打印字符串。",
    "buggy_code": "class Person:\n    def __init__(self, name):\n        self.name = name\n    def __str__(self):\n        return f\"[{self.name}]\"\n\ndef print_people():\n    people = [Person(\"Alice\"), Person(\"Bob\")]\n    return str(people)",
    "kcs": [
      {
        "kc_id": "kc_str_vs_repr_in_containers",
        "description": "理解虽然对象实现了 `__str__`，但在将列表等容器整体转换为字符串时，容器内部调用的是每个元素的 `__repr__` 方法。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_implementing_repr",
        "description": "预见由于未实现 `__repr__`，打印列表会返回难以阅读的内存地址 `<__main__.Person object at 0x...>`。必须重写 `__repr__(self)` 或让 `__repr__ = __str__` 以供调试。",
        "prerequisites": ["kc_str_vs_repr_in_containers"]
      }
    ]
  },
  {
    "id": "task_079_multi_debug",
    "problem_description": "定义一个 `Game` 类，包含类变量 `score = 0`。实例化两个玩家，并编写函数让玩家 A 的分数增加 10，然后返回类全局的分数。",
    "buggy_code": "class Game:\n    score = 0\n\ndef add_score():\n    player_a = Game()\n    player_b = Game()\n    player_a.score += 10\n    return Game.score",
    "kcs": [
      {
        "kc_id": "kc_augmented_assignment_shadowing",
        "description": "理解 `player_a.score += 10` 等价于 `player_a.score = player_a.score + 10`。它首先读取了类变量的值 (0)，然后在实例字典中创建了一个同名的局部实例变量 `score = 10`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_class_variable_access",
        "description": "理解这一操作发生了变量遮蔽 (Shadowing)，原有的类全局变量 `Game.score` 并未被修改，仍然是 0。修改类变量必须显式调用类名 `Game.score += 10`。",
        "prerequisites": ["kc_augmented_assignment_shadowing"]
      }
    ]
  },
  {
    "id": "task_080_multi_debug",
    "problem_description": "编写一个函数 `safe_read(val)`。尝试将 val 解析为 JSON。无论是否发生异常，在 finally 块中尝试打印解析后的字典长度并返回成功位。",
    "buggy_code": "import json\ndef safe_read(val):\n    try:\n        data = json.loads(val)\n    except ValueError:\n        pass\n    finally:\n        print(len(data))\n        return True",
    "kcs": [
      {
        "kc_id": "kc_variable_assignment_failure",
        "description": "理解在 `try` 块中，如果 `json.loads(val)` 抛出异常，赋值操作 `data = ...` 根本不会发生。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_nameerror_in_finally",
        "description": "预见由于变量未被初始化，在 `finally` 块中强行访问 `len(data)` 会引发 NameError: name 'data' is not defined。必须在 try 外部预先初始化 `data = {}` 或在 finally 前增加状态判断。",
        "prerequisites": ["kc_variable_assignment_failure"]
      }
    ]
  },
  {
    "id": "task_081_multi_debug",
    "problem_description": "编写一个函数 `initialize_inventory(items)`，接收一个物品名称列表，使用 `dict.fromkeys()` 将它们初始化为空列表，并向第一个物品中添加一个默认数量 1。",
    "buggy_code": "def initialize_inventory(items):\n    inventory = dict.fromkeys(items, [])\n    if items:\n        inventory[items[0]].append(1)\n    return inventory",
    "kcs": [
      {
        "kc_id": "kc_fromkeys_mutable_reference",
        "description": "理解 `dict.fromkeys(keys, value)` 会将同一个 `value` 对象的引用分配给所有的键。传入可变对象 `[]` 会导致所有键共享同一个列表。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_dict_comprehension_instantiation",
        "description": "掌握为每个键独立创建可变对象的正确方法：放弃 `fromkeys`，改用字典推导式 `{k: [] for k in items}`。",
        "prerequisites": ["kc_fromkeys_mutable_reference"]
      }
    ]
  },
  {
    "id": "task_082_multi_debug",
    "problem_description": "编写一个函数 `count_lines_and_words(filepath)`。先遍历文件统计总行数，再遍历一次文件统计总单词数，最后返回元组 `(lines, words)`。",
    "buggy_code": "def count_lines_and_words(filepath):\n    with open(filepath, 'r') as f:\n        lines = sum(1 for _ in f)\n        words = sum(len(line.split()) for line in f)\n    return (lines, words)",
    "kcs": [
      {
        "kc_id": "kc_file_iterator_exhaustion",
        "description": "理解文件对象 `f` 本质上是一个单向迭代器。在计算 `lines` 时游标已经跑到了文件末尾（EOF），第二次计算 `words` 时读取不到任何内容，结果永远为 0。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_file_seek_reset",
        "description": "掌握重置文件游标的机制：在第二次遍历之前，必须显式调用 `f.seek(0)` 将读取指针移回文件头部。",
        "prerequisites": ["kc_file_iterator_exhaustion"]
      }
    ]
  },
  {
    "id": "task_083_multi_debug",
    "problem_description": "编写一个函数 `remove_all_zeros(nums)`，移除列表中所有的 0，返回修改后的原列表。",
    "buggy_code": "def remove_all_zeros(nums):\n    if 0 in nums:\n        nums.remove(0)\n    return nums",
    "kcs": [
      {
        "kc_id": "kc_list_remove_first_only",
        "description": "理解 `list.remove(val)` 方法的作用域局限性：它只会寻找并移除列表中【第一个】匹配的元素，并不会清理后续的同名元素。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_loop_remove_or_comprehension",
        "description": "掌握批量删除元素的正确姿势：可以使用 `while 0 in nums: nums.remove(0)`，或者更现代的方法——直接使用列表推导式覆盖 `nums[:] = [n for n in nums if n != 0]`。",
        "prerequisites": ["kc_list_remove_first_only"]
      }
    ]
  },
  {
    "id": "task_084_multi_debug",
    "problem_description": "编写一个函数 `build_file_path(base_dir, filename)`，将基础目录（如 `\"/var/log\"`）和文件名拼接成完整路径。注意，上游传入的文件名可能意外带了前导斜杠（如 `\"/error.log\"`）。",
    "buggy_code": "import os\ndef build_file_path(base_dir, filename):\n    return os.path.join(base_dir, filename)",
    "kcs": [
      {
        "kc_id": "kc_os_path_join_absolute",
        "description": "理解 `os.path.join` 的隐蔽规则：如果遇到任何以 `/` 开头的参数，它会将其视为绝对路径，并无情地丢弃之前拼接的所有目录。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_lstrip_path_separator",
        "description": "预见 `/error.log` 会让结果直接变成 `/error.log` 而非 `/var/log/error.log`。必须在拼接前对 `filename` 进行清洗：`filename.lstrip('/')`。",
        "prerequisites": ["kc_os_path_join_absolute"]
      }
    ]
  },
  {
    "id": "task_085_multi_debug",
    "problem_description": "编写一个函数 `contains_nan(nums)`，检查浮点数列表中是否包含 NaN (Not a Number) 值。如果包含返回 True。",
    "buggy_code": "def contains_nan(nums):\n    for n in nums:\n        if n == float('nan'):\n            return True\n    return False",
    "kcs": [
      {
        "kc_id": "kc_nan_inequality",
        "description": "理解 IEEE 754 浮点数标准的底层强制规定：NaN 永远不等于任何值，甚至不等于它自己。因此 `n == float('nan')` 永远会返回 False。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_math_isnan",
        "description": "掌握判断 NaN 的专有方法：必须引入标准库并调用 `math.isnan(n)`，或者利用它不等于自身的特质 `if n != n:` 进行极客判断。",
        "prerequisites": ["kc_nan_inequality"]
      }
    ]
  },
  {
    "id": "task_086_multi_debug",
    "problem_description": "定义一个 `Counter` 类，包含一个 `increment()` 方法用于将实例内部的计数器加一。该代码目前在被调用时会报错。",
    "buggy_code": "class Counter:\n    def __init__(self):\n        self.count = 0\n    def increment():\n        self.count += 1",
    "kcs": [
      {
        "kc_id": "kc_method_missing_self",
        "description": "理解在 Python 中，通过实例调用方法（如 `obj.increment()`）时，解释器会自动将该实例对象作为第一个参数静默传入。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_self_parameter_definition",
        "description": "预见由于 `increment` 没定义任何参数，接收到实例对象时会触发 TypeError (takes 0 positional arguments but 1 was given)。必须在参数列表加上 `self`。",
        "prerequisites": ["kc_method_missing_self"]
      }
    ]
  },
  {
    "id": "task_087_multi_debug",
    "problem_description": "编写一个函数 `add_new_users(existing_users, new_users)`，接收两个集合(Set)。将新用户全部加入已有用户集合中，并返回修改后的已有用户集合。",
    "buggy_code": "def add_new_users(existing_users, new_users):\n    existing_users.union(new_users)\n    return existing_users",
    "kcs": [
      {
        "kc_id": "kc_set_union_returns_new",
        "description": "理解集合的 `.union()` 方法并不修改调用它的原集合，而是执行数学上的并集运算并返回一个全新的集合对象。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_set_update_inplace",
        "description": "掌握在原集合上就地添加元素的正确语法：应当使用 `existing_users.update(new_users)`，或者将 union 的返回值重新赋值 `existing_users = ...`。",
        "prerequisites": ["kc_set_union_returns_new"]
      }
    ]
  },
  {
    "id": "task_088_multi_debug",
    "problem_description": "编写一个函数 `swap_a_and_b(text)`，将字符串中的字母 'A' 替换为 'B'，同时将 'B' 替换为 'A'。例如 'ABC' 应变为 'BAC'。",
    "buggy_code": "def swap_a_and_b(text):\n    text = text.replace('A', 'B')\n    text = text.replace('B', 'A')\n    return text",
    "kcs": [
      {
        "kc_id": "kc_sequential_replace_cascading",
        "description": "理解链式或顺序执行 `.replace()` 会产生灾难性的雪崩效应：第一行将所有 'A' 变成了 'B'，第二行又把原本的 'B' 和新变的 'B' 统统变成了 'A'。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_str_translate_maketrans",
        "description": "掌握字符串多重字符同步替换的正确工具：应当使用字典映射逻辑，或利用内置的翻译表 `text.translate(str.maketrans('AB', 'BA'))`。",
        "prerequisites": ["kc_sequential_replace_cascading"]
      }
    ]
  },
  {
    "id": "task_089_multi_debug",
    "problem_description": "编写一个函数 `double_elements(lst)`，原地将整数列表中的每个元素翻倍。例如输入 `[2, 0, 1]` 应返回 `[4, 0, 2]`。",
    "buggy_code": "def double_elements(lst):\n    for item in lst:\n        lst[item] = item * 2\n    return lst",
    "kcs": [
      {
        "kc_id": "kc_for_loop_yields_elements_not_indices",
        "description": "理解 `for item in lst:` 每次解包拿到的是列表的【元素值】，而不是【索引】。将元素值直接当作索引访问 `lst[item]` 会导致严重的下标错乱甚至 IndexError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_enumerate_for_mutation",
        "description": "掌握在循环中原地修改列表的最佳实践：必须使用 `enumerate` 获取真实索引，写为 `for idx, val in enumerate(lst): lst[idx] = val * 2`。",
        "prerequisites": ["kc_for_loop_yields_elements_not_indices"]
      }
    ]
  },
  {
    "id": "task_090_multi_debug",
    "problem_description": "编写一个函数 `check_any_success(funcs)`，接收一个无参函数列表。只要其中一个函数返回 True，就立刻返回 True 并停止执行后续函数（以防后续函数报错）。",
    "buggy_code": "def check_any_success(funcs):\n    return any([f() for f in funcs])",
    "kcs": [
      {
        "kc_id": "kc_list_comprehension_eager_evaluation",
        "description": "理解列表推导式 `[...]` 是贪婪求值 (Eager Evaluation) 的。它会强行把所有函数都执行一遍并组装成一个布尔值列表，彻底破坏了 `any()` 期望的短路保护特性。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_generator_expression_lazy_evaluation",
        "description": "掌握让 `any()` 实现短路求值的秘密：去除列表推导式的中括号，将其变为生成器表达式 `any(f() for f in funcs)`，实现惰性执行。",
        "prerequisites": ["kc_list_comprehension_eager_evaluation"]
      }
    ]
  },
  {
    "id": "task_091_multi_debug",
    "problem_description": "编写一个函数 `get_time_diff(dt_aware, dt_naive)`，计算两个 datetime 对象的时间差。一个是带时区的 (aware)，一个是不带时区的 (naive)。如果直接相减会报错。",
    "buggy_code": "def get_time_diff(dt_aware, dt_naive):\n    return dt_aware - dt_naive",
    "kcs": [
      {
        "kc_id": "kc_datetime_tzinfo_mismatch",
        "description": "理解在 Python 的 datetime 模块中，带时区 (Aware) 和不带时区 (Naive) 的时间对象属于不同的时间空间，直接使用 `-` 运算符相减会触发 TypeError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_datetime_replace_tzinfo",
        "description": "掌握抹平时间空间的正确方法：必须通过 `dt_naive.replace(tzinfo=...)` 为其添加时区，或者通过 `dt_aware.replace(tzinfo=None)` 剥离时区后，才能进行计算。",
        "prerequisites": ["kc_datetime_tzinfo_mismatch"]
      }
    ]
  },
  {
    "id": "task_092_multi_debug",
    "problem_description": "定义一个 `Student` 类，包含 `name` 和 `age` 属性。编写一个函数 `get_youngest(students)`，接收该类对象的列表，并使用 `min()` 函数返回年龄最小的学生对象。",
    "buggy_code": "class Student:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\ndef get_youngest(students):\n    return min(students)",
    "kcs": [
      {
        "kc_id": "kc_min_requires_less_than",
        "description": "理解内置的 `min()` 和 `max()` 函数在比较自定义对象时，底层会隐式调用对象的 `<` (小于) 运算符。如果类没有实现该运算符，会抛出 TypeError。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_min_key_function",
        "description": "掌握对自定义对象排序或极值计算的方案：要么在类中实现 `__lt__(self, other)` 魔术方法，要么在调用时显式指定排序键 `min(students, key=lambda s: s.age)`。",
        "prerequisites": ["kc_min_requires_less_than"]
      }
    ]
  },
  {
    "id": "task_093_multi_debug",
    "problem_description": "使用 `@dataclass` 定义一个 `Inventory` 数据类，包含一个默认值为空列表的 `items` 属性。",
    "buggy_code": "from dataclasses import dataclass\n\n@dataclass\nclass Inventory:\n    items: list = []",
    "kcs": [
      {
        "kc_id": "kc_dataclass_mutable_default_guard",
        "description": "理解 Python 的 `dataclasses` 模块自带防御机制。为了防止所有实例共享同一个可变默认对象，它在定义阶段就会直接抛出 ValueError 拒绝这种写法。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_dataclass_default_factory",
        "description": "掌握数据类中可变默认属性的规范写法：必须使用 `field` 对象，并传入无参工厂函数，写为 `items: list = field(default_factory=list)`。",
        "prerequisites": ["kc_dataclass_mutable_default_guard"]
      }
    ]
  },
  {
    "id": "task_094_multi_debug",
    "problem_description": "编写一个函数 `count_positives(nums)`。使用内置的 `filter()` 筛选出所有正数，然后使用 `len()` 函数返回其数量。",
    "buggy_code": "def count_positives(nums):\n    positives = filter(lambda x: x > 0, nums)\n    return len(positives)",
    "kcs": [
      {
        "kc_id": "kc_filter_returns_iterator",
        "description": "理解在 Python 3 中，`filter()` 函数返回的是一个惰性求值的过滤器对象 (Iterator)，而不是 Python 2 中的列表。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_iterator_no_len",
        "description": "理解迭代器对象在内存中并不提前知道包含多少元素，因此它不支持 `len()` 函数，会引发 TypeError。必须使用 `len(list(positives))` 强制物化为列表。",
        "prerequisites": ["kc_filter_returns_iterator"]
      }
    ]
  },
  {
    "id": "task_095_multi_debug",
    "problem_description": "编写一个函数 `extract_error(log_text)`。从一行日志中提取 'ERROR' 关键字及之后的所有文本。例如对于输入 \"[2026-01-01] ERROR: disk full\"，应返回匹配对象，没找到则返回 None。",
    "buggy_code": "import re\ndef extract_error(log_text):\n    return re.match(r'ERROR.*', log_text)",
    "kcs": [
      {
        "kc_id": "kc_re_match_anchors_start",
        "description": "理解正则表达式库中 `re.match()` 函数的严苛限制：它强制要求模式必须从字符串的【绝对起始位置】开始匹配，否则返回 None。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_re_search_anywhere",
        "description": "掌握在字符串中间进行模式扫描的正确函数：应当换用 `re.search()` 才能实现全局扫描找到目标关键字。",
        "prerequisites": ["kc_re_match_anchors_start"]
      }
    ]
  },
  {
    "id": "task_096_multi_debug",
    "problem_description": "编写一个函数 `create_user(uid, **kwargs)`。在调用该函数时，不小心在常规关键字参数和解包字典中传入了相同的键 `uid`。",
    "buggy_code": "def create_user(uid, **kwargs):\n    return {\"uid\": uid, **kwargs}\n\n# 外部调用示例：\n# payload = {'uid': 102, 'name': 'Bob'}\n# create_user(uid=101, **payload)",
    "kcs": [
      {
        "kc_id": "kc_kwargs_unpacking_expansion",
        "description": "理解 `**payload` 在函数调用时会被就地展开为独立的关键字参数，即等价于 `uid=101, uid=102, name='Bob'`。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_duplicate_keyword_arguments",
        "description": "预见 Python 语法严格禁止向同一个参数传入多次值，这会直接导致 TypeError: got multiple values for keyword argument 'uid'。必须在传入前剔除字典中的冲突键。",
        "prerequisites": ["kc_kwargs_unpacking_expansion"]
      }
    ]
  },
  {
    "id": "task_097_multi_debug",
    "problem_description": "定义一个 `Temperature` 类。使用 `@property` 装饰器将 `celsius` 方法伪装成属性。随后尝试对其进行直接赋值 `temp.celsius = 25`。",
    "buggy_code": "class Temperature:\n    def __init__(self):\n        self._c = 0\n    @property\n    def celsius(self):\n        return self._c\n\ndef update_temp(temp):\n    temp.celsius = 25",
    "kcs": [
      {
        "kc_id": "kc_property_getter_only",
        "description": "理解单独使用 `@property` 装饰器只会为该方法生成一个只读的 getter 描述符。任何试图向其赋值的行为都会触发 AttributeError: can't set attribute。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_property_setter_decorator",
        "description": "掌握实现属性赋值钩子的规范做法：必须再定义一个同名方法，并使用对应的 setter 装饰器修饰它（例如 `@celsius.setter`）。",
        "prerequisites": ["kc_property_getter_only"]
      }
    ]
  },
  {
    "id": "task_098_multi_debug",
    "problem_description": "编写一个函数 `round_price(price)`，使用 `round()` 将浮点数价格保留两位小数。发现测试用例 `round(2.675, 2)` 的结果不符合预期的 2.68。",
    "buggy_code": "def round_price(price):\n    return round(price, 2)",
    "kcs": [
      {
        "kc_id": "kc_float_ieee754_representation",
        "description": "理解由于浮点数在底层的二进制表示存在舍入误差，`2.675` 在内存中的实际值可能略微小于它自己（例如 `2.674999...`），导致它无法进位而是变成了 2.67。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_decimal_module",
        "description": "掌握金融与商业计算中精确四舍五入的唯一解决方案：放弃内置 float，转而导入 `from decimal import Decimal`，并在字符串层面进行精准构造与量化。",
        "prerequisites": ["kc_float_ieee754_representation"]
      }
    ]
  },
  {
    "id": "task_099_multi_debug",
    "problem_description": "定义一个 `Node` 类。为了方便比较节点是否相同，重写了 `__eq__` 方法。接着尝试将多个 Node 对象放入一个集合 (Set) 中去重，却发生了崩溃。",
    "buggy_code": "class Node:\n    def __init__(self, id):\n        self.id = id\n    def __eq__(self, other):\n        return self.id == other.id\n\ndef collect_nodes():\n    s = set()\n    s.add(Node(1))\n    return s",
    "kcs": [
      {
        "kc_id": "kc_eq_override_disables_hash",
        "description": "理解 Python 数据模型的隐蔽规则：如果一个类重写了 `__eq__` 但没有重写 `__hash__`，解释器会隐式地将其 `__hash__` 设为 None，使其变成不可哈希类型 (Unhashable)。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_implement_hash_method",
        "description": "掌握使其重新支持 Set 或 Dict 键的方案：必须同步实现哈希方法，以保证相等对象的哈希值必定相等（例如 `def __hash__(self): return hash(self.id)`）。",
        "prerequisites": ["kc_eq_override_disables_hash"]
      }
    ]
  },
  {
    "id": "task_100_multi_debug",
    "problem_description": "编写一个函数 `execute_dynamic(code_str)`，接收一段用户传入的代码字符串（例如 `\"x = 10\"`）并使用 `eval()` 执行它以动态生成变量。",
    "buggy_code": "def execute_dynamic(code_str):\n    local_vars = {}\n    eval(code_str, {}, local_vars)\n    return local_vars",
    "kcs": [
      {
        "kc_id": "kc_eval_expression_only",
        "description": "理解内置的 `eval()` 函数只能计算和执行合法的 Python【表达式】(Expression，即能求出一个值的代码)，绝不允许执行赋值语句等【声明/语句】(Statement)。",
        "prerequisites": []
      },
      {
        "kc_id": "kc_exec_for_statements",
        "description": "掌握动态执行语句的正确函数：对于如 `x = 10` 或 `import sys` 这样的复杂语句块，必须改用 `exec()` 函数。",
        "prerequisites": ["kc_eval_expression_only"]
      }
    ]
  }
]